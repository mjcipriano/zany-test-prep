# Test configuration. The `screenshots` tag marks the docs screenshot generator
# (test/screenshots/), which depends on local font files and writes PNGs. It is
# excluded in CI via: flutter test --exclude-tags screenshots
tags:
  screenshots:
    description: Generates docs/screenshots PNGs; run locally, skipped in CI.
