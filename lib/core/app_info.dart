/// App-wide constants that aren't tied to any layer.
///
/// [kAppVersion] is the human-facing app version shown on the About screen and
/// stamped into exported backups. Keep it in sync with `version:` in
/// pubspec.yaml when cutting a release (see docs/release.md).
library;

const String kAppVersion = '1.7.6';
