# Content review — progress tracker

A section-at-a-time audit + improvement of the SAT content. Work one section per
pass, commit, then move to the next so progress survives interruptions. **This
file is the source of truth for where we are.**

Content is generated by Python (`tools/content_gen/`), so "fixing a section"
means fixing its generator(s) + teaching, then regenerating:

```bash
source scripts/activate.sh
python tools/generate_content.py        # regenerate content/exams/sat/**
python tools/validate_content.py        # must pass (no errors); minimize warnings
python tools/build_bundle.py            # rebuild assets/content/sat.bundle.json
flutter test                            # keep green
```

A "section" = one skill across its easy/medium/hard tiers.

## Definition of done (per section)

1. **Difficulty ramps** — easy → medium → hard get *substantially* harder
   (bigger/uglier numbers, more steps, more abstraction), not just relabeled.
2. **No answer tells** — correct vs. wrong answers show no pattern: not always the
   same position (validator enforces), not always longest, and **not always the
   same first word / wording** (validator: `answer_wording_flags`). Distractors are
   plausible.
3. **Variation + trickier distractors** — multiple prompt phrasings and answer
   wordings; harder tiers use common-mistake distractors (sign errors, off-by-one,
   half/double, swapped operations, etc.).
4. **Coverage** — add missing question types/sub-skills the real SAT tests.
5. **Figures** — add an image/figure where the SAT would show one (geometry,
   scatterplots, some data analysis). Needs the figure infra (see Phase 0).
6. **Mini-lesson** — a 2–5 screen teaching intro that *teaches* how to solve, not
   just quizzes (the lesson's `teaching_card.screens`).

## Phase 0 — shared infrastructure

- [x] **Multi-screen mini-lessons** — `teaching_card.screens` (2–5) in schema +
  `TeachingScreen`/`TeachingCard.screens` model + paged `_TeachingView` UI
  (Back/Next/dots, Start on last). Backward compatible: a legacy single card
  renders as one screen. Authoring lives in `tools/content_gen/teaching.py`.
- [x] **Answer-wording validator** — `tools/validate_content.py` flags per-section
  first-word tells (`answer_wording_flags`) in addition to position/length tells.
- [ ] **Figure / image support** — add a `figure` stimulus (schema + `Stimulus`
  model + a CustomPainter/SVG renderer) so geometry/scatterplot items can show a
  diagram. Implement when the first figure-needing section is reached
  (right_triangles / circles / area_volume / scatterplots / data_analysis).

## Known answer-wording offenders (from the validator, fix during their section)

- `pronoun_antecedent` easy/medium/hard — correct answers nearly all start with
  "its"/"their".
- `quantitative_evidence` easy/medium/hard — correct answers all start with "each".

## Sections

Order = top to bottom. Mark `[x]` when a section meets the Definition of Done.

### Reading & Writing
- [ ] main_idea
- [ ] inferences
- [ ] command_of_evidence
- [ ] quantitative_evidence  ⚠ wording tell
- [ ] words_in_context
- [ ] text_structure_purpose
- [ ] cross_text_connections
- [ ] transitions
- [ ] rhetorical_synthesis
- [ ] concision
- [ ] sentence_boundaries
- [ ] subject_verb_agreement
- [ ] punctuation
- [ ] pronouns
- [ ] modifiers
- [ ] verb_tense
- [ ] parallel_structure
- [ ] pronoun_antecedent  ⚠ wording tell

### Math
- [ ] linear_equations
- [ ] linear_inequalities
- [ ] systems_of_equations
- [ ] word_problems
- [ ] absolute_value
- [ ] functions
- [ ] quadratics
- [ ] exponents_radicals
- [ ] polynomials
- [ ] exponential
- [ ] ratios_percentages_units
- [ ] proportions
- [ ] data_analysis  (figure?)
- [ ] statistics
- [ ] probability
- [ ] two_way_tables
- [ ] scatterplots  (figure)
- [ ] right_triangles  (figure)
- [ ] area_volume  (figure)
- [ ] circles  (figure)
- [ ] trigonometry  (figure)

## Resume prompt

> Continue the content review in `docs/content-review.md`. Do the next unchecked
> section (top of the list): review its generator(s) in `tools/content_gen/` for
> the 6 Definition-of-Done criteria, add a 2–5 screen mini-lesson in
> `teaching.py`, fix any answer-wording/position tells, add trickier distractors
> and variation, and add a figure if the SAT would show one (build the Phase-0
> figure infra if not done yet). Then regenerate + validate + build bundle + run
> flutter tests, tick the box, and commit. One section, then stop.
