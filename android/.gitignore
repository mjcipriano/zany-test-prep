gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
# The stable release key (android/app/zany-release.jks) and android/key.properties
# stay OUT of git — back them up securely (see docs/release.md). build.gradle.kts
# uses them automatically when present, and falls back to debug signing otherwise.
key.properties
**/*.keystore
**/*.jks
